# ===========================================
# alteran.tech — Environment Variables
# ===========================================
# Copy this file to .env.local and fill in real values:
#   cp .env.local.example .env.local
#
# NEVER commit .env.local to version control.
# ===========================================

# --- Database (local SQLite) ---
# Path to SQLite file. Default: file:./alteran.db (no setup needed)
DATABASE_URL=file:./alteran.db

# --- Admin Authentication ---
# Password used to log in to /admin
ADMIN_PASSWORD=your-secure-password

# Secret for HMAC-signed session tokens (random string, min 32 chars)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-random-32-char-secret

# --- GitHub API (optional) ---
# Personal Access Token for GitHub GraphQL API (repo import feature)
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo (read-only)
GITHUB_TOKEN=

# --- OpenRouter LLM API (optional) ---
# Get API key at: https://openrouter.ai/keys
# Used for AI-generated project descriptions
OPENROUTER_API_KEY=

# --- Site Configuration ---
# Public URL of the deployed site (used for SEO, Open Graph, sitemap)
NEXT_PUBLIC_SITE_URL=https://alteran.tech
